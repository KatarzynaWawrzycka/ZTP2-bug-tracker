{% include 'default/_form.html.twig' with {
    page_title: 'title.change_password'|trans({'%id%': user.id|default('')}),
    form: form,
    submit_label: 'action.change'|trans,
    back_to_list_path: 'user_profile',
} %}
